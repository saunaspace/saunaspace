
{{- 'section-openings.css' | asset_url | stylesheet_tag -}}

<div class="openings" id="openings">
  <div class="container">
    {% if section.settings.heading != blank %}
      <div
        class="openings__header"
        {% if section.settings.background_image != blank %}
          style="background-image: url('{{ section.settings.background_image | img_url: 'master' }}');"
        {% endif %}
      >
        <h2 class="openings__heading">
          {{ section.settings.heading }}
        </h2>
      </div><!-- /.openings__header -->
    {% endif %}

    {% if section.blocks.size > 0 %}
      <ul class="openings__list">
        {%- for block in section.blocks -%}
          {%- assign _block = block.settings -%}

          {%- if _block.title != blank -%}
            <li
              class="openings__item"
              {{ block.shopify_attributes }}
            >
              <div class="item-opening">
                {%- if _block.link != blank -%}
                <a href="{{ _block.link }}" class="item-opening__link"{% if _block.link contains "http" %}{% unless _block.link contains "sauna.space" %} target="_blank"{% endunless %}{% endif %}>
                    <span class="visually-hidden">{{ _block.title }}</span><!-- /.visually-hidden -->
                  </a><!-- /.item-opening__link -->
                {%- endif -%}

                <div class="item-opening__content">
                  <h5 class="item-opening__title">
                    {{- block.settings.title | escape -}}
                  </h5><!-- /.item-opening__title -->

                  {%- if _block.location != blank -%}
                    <div class="item-opening__location">
                      <i>
                        {%- render 'icon-pin', -%}
                      </i>

                      <span>
                        {{- _block.location -}}
                      </span>
                    </div><!-- /.item-opening__location -->
                  {%- endif -%}
                </div><!-- /.item-opening__content -->

                <span class="item-opening__arrow">
                  {%- render 'icon-chevron-down-thin' -%}
                </span><!-- /.item-opening__arrow -->
              </div><!-- /.item-opening -->
            </li>
          {%- endif -%}
        {%- endfor -%}
      </ul>
    {% endif %}
  </div>
</div>

{% schema %}
{
  "name": "Openings",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "Current Openings"
    },
    {
      "type": "image_picker",
      "id": "background_image",
      "label": "Background Image"
    }
  ],
  "blocks": [
    {
      "type": "opening",
      "name": "Opening",
      "settings": [
        {
          "type": "text",
          "id": "title",
          "label": "Title",
          "default": "Title"
        },
        {
          "type": "text",
          "id": "location",
          "label": "Location",
          "default": "Location"
        },
        {
          "type": "url",
          "id": "link",
          "label": "Link"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "Openings",
      "blocks": [
        {
          "type": "opening",
        },
        {
          "type": "opening",
        }
      ]
    }
  ]
}
{% endschema %}
