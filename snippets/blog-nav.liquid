<div class="navbar-inner blog-nav">
  <div class="container">

    <div class="navbar-center-wrapper">
      <div class="navbar-collapse collapse">
        <ul id="menu-header-menu-1" class="nav">
          <li id="mgt-menu-item-613" class="menu-item menu-item-type-taxonomy menu-item-object-category">
            <a title="Blog Home" href="/blogs/the-wavelength/">Blog Home</a>
          </li>
          <li id="mgt-menu-item-612" class="menu-item menu-item-type-taxonomy menu-item-object-category">
            <a title="Science" href="/blogs/the-wavelength/tagged/science/">Science</a>
          </li>
          <li id="mgt-menu-item-472" class="menu-item menu-item-type-taxonomy menu-item-object-category">
            <a title="Products" href="/blogs/the-wavelength/tagged/products/">Products</a>
          </li>
          <li id="mgt-menu-item-611" class="menu-item menu-item-type-taxonomy menu-item-object-category">
            <a title="Lifestyle" href="/blogs/the-wavelength/tagged/lifestyle/">Lifestyle</a>
          </li>
        </ul>
      </div>
    </div>
  </div>
</div>


<style>
  @media(min-width: 991px) {
    header.banner {
      display: none;
    }
  }

  @media(max-width: 990px) {
    .navbar-inner {
      display: none;
    }

    .blog-nav ul li {
      display: inline-block;
      padding: 10px;
    }
    .blog-nav .container {
      width: 100% !important;
    }
  }

  @media(max-width: 768px) {
    .blog-nav ul li {
      display: inline-block;
      padding: 8px 0;
    }
    .blog-nav ul li a {
      padding: 2px 10px;
      font-size: 14px;
    }
  }
  /**
 * Hidden fallback
 */
  [hidden] {
    display: none !important;
    visibility: hidden;
  }
  /**
 * Styling navigation
 */
  header.banner {
    margin-right: auto;
    margin-left: auto;
    max-width: 100%;
    background: #6d1c0f;
    box-shadow: 0 3px 12px rgba(0, 0, 0, 0.25);
  }

  header.banner nav {
    display: block;
  }

  header.banner nav ul {
    text-align: center;
  }

  /**
 * Styling top level items
 */
  .nav a,
  .nav label {
    display: block;
    padding: 0.85rem;
    color: #fff;
    transition: all 0.25s ease-in;
  }
  .nav label {
    cursor: pointer;
  }
  /**
 * Styling first level lists items
 */
  .group-list a,
  .group-list label {
    border-bottom: 1px solid #eee;
    display: block;
    text-decoration: none;
  }

  .nav__list li {
    list-style: none;
    margin-left: 0;
    display: block;
  }
  /**
 * Styling second level list items
 */
  .sub-group-list a,
  .sub-group-list label {
    padding-left: 4rem;
    background: #353535;
  }
  /**
 * Styling third level list items
 */
  .sub-sub-group-list a,
  .sub-sub-group-list label {
    padding-left: 6rem;
    background: #454545;
  }
  /**
 * Hide nested lists
 */
  .group-list,
  .sub-group-list,
  .sub-sub-group-list {
    height: 100%;
    max-height: 0;
    overflow: hidden;
    transition: max-height 0.5s ease-in-out;
  }

  .nav__list input[type=checkbox]:checked + label + ul {
    /* reset the height when checkbox is checked */
    max-height: 1000px;
  }
  /**
 * Rotating chevron icon
 */
  label > span {
    float: right;
    transition: transform 0.65s ease;
  }
  .nav__list input[type=checkbox]:checked + label > span {
    transform: rotate(90deg);
  }
</style>

<script>

  // Cache elements for performance
  const mainHeader = document.getElementById('shopify-section-header');
  const subMenu = document.getElementsByClassName('blog-nav');

  // Function to update sub-menu position
  function updateSubMenuPosition() {
    const mainHeaderHeight = mainHeader.offsetHeight;

    // Update sub-menu top position
    subMenu.style.top = `${mainHeaderHeight}px`;
  }

  // Attach scroll event
  window.addEventListener('scroll', updateSubMenuPosition);

  // Initial position update
  updateSubMenuPosition();
</script>